# Core: loaders + controller + API
psycopg2-binary
requests
python-dotenv
pandas
yfinance
websockets
fastapi
uvicorn[standard]
networkx

# Scenario Modeller (scenario/engine.py): Monte Carlo jump-diffusion
numpy
scipy

# Agent pipeline (agents/graph.py): linear LangGraph StateGraph over the
# scenario + procurement logic above
langgraph

# LLM extraction stages (extract_events.py / hybrid_extract.py) — free-tier OpenRouter
openai

# Tests (tests/, run by .github/workflows/ci.yml)
pytest
httpx  # fastapi TestClient

# Only for the BigQuery GDELT path (gdelt.py / gkg.py); the default public
# path doesn't need it. Uncomment if you have GCP credentials:
# google-cloud-bigquery
# db-dtypes
